# Slimefun 自动合成助手

> 基于 JsMacros 的粘液科技配方读取、材料计算与自动合成工具。

## 项目信息

| 项目 | 内容 |
| --- | --- |
| 当前版本 | V4.6.0（Jerinin 维护版） |
| 维护者 | Jerinin |
| 更新日期 | 2026-07-28 |
| 许可证 | MIT License |
| 运行环境 | Minecraft 客户端 + JsMacros |

### 维护版说明

本仓库发布的是由 Jerinin 持续维护的兼容版本。为避免失效群组、旧联系方式或无法持续维护的外部入口误导使用者，维护版仅保留当前仓库、Release 页面和必要的官方依赖链接作为支持渠道。历史贡献与项目许可信息仍按仓库中的 `LICENSE` 保留，此调整不改变原项目的许可证归属，也不代表对历史贡献的否定。

## 主要功能

- 从粘液科技指南读取并保存配方
- 根据目标物品自动计算材料依赖和合成顺序
- 扫描附近工作站与存储容器
- 自动补充缺失材料并执行合成
- 在 HUD 中显示制作列表、缺失材料和合成队列
- 使用黑名单排除不希望自动处理的配方
- 通过网页管理器编辑和导出工作站配置

## 必装前置 Mod

粘液助手是 JsMacros 使用的 JavaScript 脚本，**不是可以直接放进 `mods` 文件夹的 Mod**。运行本项目必须先在 Minecraft 客户端安装 JsMacros：

- [JsMacros 官方 GitHub Releases](https://github.com/JsMacros/JsMacros/releases)
- [JsMacros 官方 Modrinth 页面](https://modrinth.com/mod/jsmacros/versions)
- [JsMacros 官方文档](https://jsmacros.wagyourtail.xyz/)

下载时必须同时匹配 Minecraft 版本和 Mod 加载器。Fabric 客户端选择 Fabric 文件，Forge 客户端选择 Forge 文件；不匹配的 JAR 可能无法加载或导致游戏启动失败。

### 安装 JsMacros

1. 确认当前 Minecraft 实例使用的游戏版本以及 Fabric/Forge 加载器。
2. 从上方官方地址下载完全匹配的 JsMacros `.jar`。
3. 将该 JAR 放进当前 Minecraft 实例的 `mods` 文件夹。
4. 启动游戏，并确认可以打开 JsMacros 的配置或宏界面。

只有完成以上步骤后，客户端才具备导入和运行 `.js` 脚本的能力。

## 安装粘液助手

1. 打开项目的 [v4.6.0 标签页](https://github.com/fxjcangku/jerinin-addon/releases/tag/v4.6.0)，下载 GitHub 提供的 `Source code (zip)`；如果后续提供独立附件，则优先使用粘液助手发布包。
2. 解压文件，并保留 `jsm_scripts/slimefun-helper` 中完整的粘液助手文件夹结构。
3. 从游戏内 JsMacros 界面打开脚本/宏目录，避免误放到其他 Minecraft 实例。
4. 将整个“粘液助手”文件夹放入该目录，不要只复制 `粘液助手 V4.6.0.js`。
5. 确认 JS 文件旁边存在 `config` 和 `html` 目录，以及其中的配置文件。
6. 在 JsMacros 中新建按键宏或事件，脚本语言选择 JavaScript，文件选择 `粘液助手/粘液助手 V4.6.0.js`。
7. 保存宏并设置运行按键。

## 使用方法

### 启动脚本

1. 进入支持粘液科技的服务器。
2. 确保粘液科技指南位于副手、主背包或快捷栏。
3. 按下为粘液助手绑定的宏按键；指南不在副手时会自动交换到副手。
4. 脚本会加载配置、初始化粘液指南、注册 HUD 和事件监听。
5. 聊天栏出现“脚本已启动”，且 HUD 显示制作列表、材料列表和合成队列，即表示启动成功。

### 停止脚本

脚本运行期间再次执行同一个脚本，即可请求停止。若脚本没有正常停止，可再次执行并按照游戏内提示强制关闭旧上下文。

### 读取配方

打开粘液科技指南后，脚本会在支持的界面中提供以下操作：

- **读取配方**：读取当前分类中的可用配方
- **读取当前配方**：只保存当前展示的配方
- **添加到黑名单**：阻止指定物品参与自动合成
- **从黑名单移除**：恢复指定物品的自动合成
- **数量+1 / 数量-1**：微调制作数量
- **数量+32 / 数量-32**：按半组调整制作数量
- **数量+64 / 数量-64**：按整组调整制作数量

数量按钮以三行两列显示，正数在左、对应负数在右。

### 自动合成

1. 在指南配方页面选择需要制作的物品和数量。
2. 确认 HUD 显示的材料列表和合成队列正确。
3. 关闭指南界面以开始自动合成。
4. 脚本会扫描附近容器和工作站，取出材料并依次合成。

首次使用时请先用少量普通材料测试，确认服务器的菜单布局、物品 ID 和工作站配置与脚本兼容。

## 文件结构

```text
粘液助手/
├─ 粘液助手 V4.6.0.js          主程序脚本
├─ 使用说明.md                 使用说明
├─ config/
│  ├─ 指南识别.json            跨服务器指南与菜单识别规则
│  ├─ 配方.json                已读取的配方
│  ├─ 工作站.json              工作站结构与槽位配置
│  └─ 黑名单.json              禁止自动合成的物品
└─ html/
   ├─ 工作站配置管理器.html    工作站配置编辑器
   ├─ js/                      页面逻辑与本地依赖
   └─ css/                     页面样式
```

## 工作站配置管理器

打开 `html/工作站配置管理器.html` 可以编辑工作站配置。

1. 使用“导入配置”选择现有的 `工作站.json`。
2. 添加、编辑或删除工作站。
3. 使用“下载 JSON”导出新的配置。
4. 将导出的文件替换到 `config/工作站.json`。
5. 重新启动脚本以加载新配置。

网页管理器不会直接修改脚本目录中的配置文件，需要手动导出并替换。请勿导入来源不明的 JSON 配置。

## 兼容性说明

- 原说明要求 JsMacros 1.9.2+、Minecraft 1.20.1+。
- 新版 Minecraft 必须使用对应版本的 JsMacros，不能混用不同游戏版本的模组文件。
- 不同服务器可能安装不同版本的 Slimefun 或附属插件，GUI 标题、槽位、物品 ID 和工作站结构可能不同。
- 如果配方读取错位、工作站无法识别或自动点击位置错误，应立即停止脚本并调整配置。
- 使用自动操作前请确认服务器规则允许此类客户端脚本。

## 注意事项

- 配置文件必须是有效的 JSON 格式。
- 配方 ID 和工作站 ID 必须与服务器中的实际 ID 一致。
- 自动合成前应预留足够的背包空间。
- 首次测试不要使用贵重材料，也不要在装满重要物品的仓库附近运行。
- 运行日志保存在 `logs/slimefun-helper.log`，每次启动会清理旧日志。

## 常见问题

### 脚本无法启动

- 检查 JsMacros 是否与当前 Minecraft 版本匹配。
- 确认指南位于副手、主背包或快捷栏，并检查 `指南识别.json` 是否包含该服务器指南的 ID、名称或 NBT 特征。
- 查看游戏聊天提示和 `logs/slimefun-helper.log`。

### 配置无法加载

- 检查四个 JSON 文件是否存在且格式正确。
- 确认整个文件夹已放入 JsMacros 脚本目录。
- 检查文件名是否保持为 `指南识别.json`、`配方.json`、`工作站.json` 和 `黑名单.json`。

### 读取不到配方

- 确认当前界面是粘液科技指南的配方页面。
- 检查服务器是否修改了指南布局或菜单物品 ID。
- 尝试先使用“读取当前配方”测试单个配方。

### 无法识别工作站

- 确认工作站位于玩家交互范围内。
- 检查工作站结构、方块 ID、GUI 名称和槽位配置。
- 服务器使用不同附属版本时，需要重新制作对应的工作站配置。

### 脚本没有响应

- 再次运行脚本以请求停止。
- 根据提示再次执行，强制关闭旧脚本上下文。
- 仍无法恢复时，重启 Minecraft 后再测试。

## 更新记录

### V4.6.0 指南兼容与操作体验更新（2026-07-28）

#### 指南兼容与副手管理

- 新增 `指南识别.json`，支持配置指南原版 ID、名称、名称关键词和 NBT 关键词
- 兼容 ViaVersion 等代理或跨版本环境下被剥离 Slimefun PDC 的指南物品
- 启动和运行期间自动从主背包或快捷栏寻找指南并放入副手
- 交换后重新校验，失败时自动恢复副手原物品

#### 配方页面兼容

- 支持按菜单物品名称识别返回、主页和搜索按钮
- 放宽分类图标与配方产物必须携带 Slimefun ID 的限制
- 保留容器类型、槽位数量和配方布局检查，避免普通容器误识别

#### 数量快捷操作

- 配方详情页新增三行两列数量按钮
- 支持 `+1/-1`、`+32/-32`、`+64/-64` 六档调整
- 按钮复用原制作列表入口，自动刷新材料计算、队列和 HUD

#### HUD 与界面

- HUD 重制为制作需求、缺失材料、合成队列三栏自适应布局
- 标题和物品名加粗，关键数量使用绿、红、蓝三色区分
- 增加实时数量、分隔线以及空列表状态提示
- 操作按钮放置在指南右侧并加入屏幕边界保护
- 修复强制停止后重新启动时旧合成 HUD 残留的问题

#### 维护信息

- 显示身份调整为“维护者 Jerinin”，明确当前版本的维护责任
- 整合支持渠道，移除失效、过时或无法由当前维护者持续响应的外部联系入口，避免误导使用者
- 历史贡献及许可证信息继续按仓库 `LICENSE` 保留

### V4.5.4 稳定性与界面更新（2026-07-27）

- HUD 文字统一加粗，并优化脚本信息、栏目标题和列表内容的配色
- 将指南中的配方读取与黑名单按钮移动到右侧，减少对 HUD 和视野的遮挡
- 修复材料不存在时仍被误判为投料成功的问题
- 增加零产出保护，避免自动合成无进度时无限循环
- 为切换型和分页型产物选择增加失败退出限制
- 仓储取料前重新校验实时槽位，避免缓存过期后拿错物品
- 工作站清空或产物收取失败时立即中止，避免队列状态失真
- 修复快捷合成按钮被更大批量动作覆盖的问题
- 修复背包可堆叠空间和预留空槽的计算错误
- 配置读取、创建或解析失败时明确终止初始化
- 配方与黑名单保存失败时回滚内存修改，不再错误提示成功

### V4.5.3 Jerinin 维护更新（2026-07-27）

- 维护者信息更新为 Jerinin
- 整合维护渠道，移除已过时或无法由当前维护者持续支持的群组入口与外部链接
- 修复配置写入失败仍被判断为成功的问题
- 修复等待物品时数量与超时参数错位的问题
- 修复按钮位于槽位 `0` 时被误判为无效的问题
- 修复工作站配置管理器导入恶意 JSON 时的脚本注入风险
- 整理并更新使用说明、兼容性提示和安全注意事项

### V4.5.2（2025-10-28）

- 修复最大堆叠数为 1 的物品放入工作站失败的问题
- 优化槽位容量计算，支持动态获取物品真实堆叠数
- 新增物品堆叠数缓存和槽位状态预取机制
- 优化自动合成性能并清理冗余调试日志

### V4.5.1

- 修复带合成介绍页面的配方读取
- 优化内部逻辑

### V4.5.0

- 新增工作站配置编辑器
- 新增根据配方执行快捷自动合成的功能
- 优化运行效率
